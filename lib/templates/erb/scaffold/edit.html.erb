<h2><%%=t "helpers.prompt.editing", name: <%= class_name %>.model_name.human %></h2>

<%%= render 'form' %>

<%%= link_to t("helpers.submit.show"), @<%= singular_table_name %>, class: "btn btn-primary" %>
<%%= link_to t("helpers.submit.back"), <%= plural_name %>_path, class: "btn btn-primary" %>
