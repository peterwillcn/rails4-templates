<h2><%%=t "helpers.prompt.listing", name: <%= class_name %>.model_name.human %></h2>

<table class="table table-striped">
  <tr>
    <%- attributes.each do |attribute| -%>
    <th><%%=t "activerecord.attributes.<%= singular_table_name %>.<%= attribute.name %>" %></th>
    <%- end -%>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <%% @<%= plural_name %>.each do |<%= singular_table_name %>| %>
  <tr>
    <%- attributes.each do |attribute| -%>
    <td><%%= <%= plural_name %>.<%= attribute.name %> %></td>
    <%- end -%>
    <td><%%= link_to t("helpers.submit.show"), <%= singular_table_name %> %></td>
    <td><%%= link_to t("helpers.submit.edit"), edit_<%= singular_table_name %>_path(<%= singular_table_name %>) %></td>
    <td><%%= link_to t("helpers.submit.destroy"), <%= singular_table_name %>, method: :delete, data: { confirm: t("helpers.prompt.confirm.destroy") } %></td>
  </tr>
  <%% end %>
</table>

<%%= link_to t("helpers.submit.new"), new_<%= plural_name %>_path, class: "btn btn-primary" %>
