%h2= t 'helpers.titles.editing', name: <%= class_name %>.model_name.human

= render 'form'

= link_to t('helpers.links.show'), @<%= singular_table_name %>, class: 'btn btn-primary'
\|
= link_to t('helpers.links.back'), <%= plural_name %>_path, class: 'btn btn-primary'